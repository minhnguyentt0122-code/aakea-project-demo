# HOMESTYLE — Feature Specification
*v2.1 · Confidential · Derived from BRD v4.0 — Premium Design Furniture*

---

## M01 — Authentication

---

### F-01.5 Token & Session Management

**Summary**

Issues, rotates, and revokes JWT access tokens and refresh tokens for both customer and admin sessions. Provides the token refresh endpoint and handles session revocation on logout or security events.

**Background / Context**

Every authenticated feature — from browsing trade pricing to completing a checkout — depends on this module. Short-lived access tokens (15 min) limit blast radius if stolen. Rotating refresh tokens detect theft via reuse detection. This module is the security backbone of the platform.

**User Story**

As a registered customer or admin, I want my session to persist securely across browser sessions without repeatedly re-entering my password, and to be automatically logged out if my session is compromised.

**Flow — Happy Path**

1. On login (F-01.2 / F-01.4), server issues access token (httpOnly cookie) and refresh token (httpOnly cookie).
2. Frontend makes authenticated API calls using the access token.
3. When access token nears expiry, frontend calls `POST /api/v1/auth/refresh`.
4. Server validates refresh token. If valid: issues new access token and rotates refresh token (old invalidated).
5. On logout: `POST /api/v1/auth/logout` invalidates the refresh token family.

**Flow — Edge Cases**

| Edge Case | Expected System Behaviour |
|---|---|
| Refresh token already used (reuse detected) | Entire refresh token family revoked. User logged out on all devices. Security event logged. |
| Access token expired — no valid refresh token | 401 returned. Frontend redirects to /login. |
| Admin inactivity timeout (TC-05) | Server does not issue new tokens after TC-05: 30 min inactivity. Admin redirected to /admin/login. |
| User requests logout on all devices | DELETE /api/v1/auth/sessions revokes all refresh tokens for user. |

**Business Rules**

| BR # | Rule |
|---|---|
| BR-01 | TC-01: Customer access token TTL: 15 minutes. TC-03: Admin access token TTL: 15 minutes. Stored in httpOnly, Secure, SameSite=Strict cookies. Never in localStorage. |
| BR-02 | TC-02: Customer refresh token TTL: 30 days, rotating. TC-04: Admin refresh token TTL: 8 hours, rotating. |
| BR-03 | Refresh tokens stored as bcrypt hashes in the DB (`refresh_tokens` table), not plaintext. |
| BR-04 | Refresh token reuse triggers full token family revocation across all devices. Security event written to audit log. |
| BR-05 | On password reset (F-01.3): ALL refresh tokens for the user are immediately revoked regardless of device or session. |
| BR-06 | Token rotation is atomic: old token invalidated and new token issued in a single DB transaction. |

**Input Validation Rules**

| Field | Type | Required | Validation Rules | Error Message Shown |
|---|---|---|---|---|
| refreshToken (httpOnly cookie) | JWT string | Yes (for /auth/refresh) | Valid JWT signature. Not expired. Exists in `refresh_tokens` table with `is_revoked=false`. | 401: 'Session expired. Please log in again.' |

**Permission Matrix**

| Method | Endpoint | Description | Guest | Customer | Trade | Ops | Content | Super Admin |
|---|---|---|---|---|---|---|---|---|
| **POST** | /api/v1/auth/refresh | Rotate access token | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **POST** | /api/v1/auth/logout | Revoke current refresh token | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **DELETE** | /api/v1/auth/sessions | Revoke all sessions | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **GET** | /api/v1/auth/sessions | List active sessions | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **DELETE** | /api/v1/auth/sessions/:sessionId | Revoke a specific session | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |

**UI / Display Rules**

| Condition / State | UI Component | Display Text | Colour / Style | Available Action |
|---|---|---|---|---|
| Session list page (/account/security) | Active sessions table | Device type, browser, IP, last active, current session highlighted | Standard table; current session row highlighted | 'Log out' button per row; 'Log out of all devices' button |
| Refresh token reuse detected | Full-page security alert + email sent | 'A security event was detected on your account. You have been logged out of all devices.' | Red alert banner | 'Change password' CTA |
| All sessions revoked | Success toast | 'You have been logged out of all other devices.' | Green toast | — |

**API Contract**

| Method | Endpoint | Description | Request Body / Params | Response |
|---|---|---|---|---|
| **POST** | /api/v1/auth/refresh | Rotate token | — (httpOnly cookie) | `200: new access token cookie` / `401: invalid or expired` |
| **POST** | /api/v1/auth/logout | Revoke refresh token | — (httpOnly cookie) | `200: { message }` |
| **DELETE** | /api/v1/auth/sessions | Revoke all sessions | — | `200: { revokedCount }` |
| **GET** | /api/v1/auth/sessions | List sessions | — | `200: { sessions[{ id, device, ip, lastSeen, isCurrent }] }` |
| **DELETE** | /api/v1/auth/sessions/:sessionId | Revoke session | — | `200: { message }` |

**Wireframes**

*[ Wireframes to be added by UX/UI Designer ]*

**Dependencies**

- Redis for lockout counters (TC-10, TC-11).
- `refresh_tokens` table in PostgreSQL.
- F-01.2, F-01.4 for initial token issuance.

**TC References:** TC-01 (15 min access token), TC-02 (30 day customer refresh), TC-03 (15 min admin access), TC-04 (8h admin refresh), TC-05 (30 min admin inactivity)

**Acceptance Criteria**

| # | Criteria | Test Type | Status |
|---|---|---|---|
| AC-01 | Expired access token + valid refresh token → new access token issued without re-login. | Integration | — |
| AC-02 | Refresh token reuse revokes entire family — all devices logged out. Security event logged. | Integration | — |
| AC-03 | DELETE /auth/sessions revokes all tokens; subsequent refresh returns 401. | Integration | — |
| AC-04 | GET /auth/sessions returns correct device/IP metadata and flags current session. | Integration | — |
| AC-05 | Password reset invalidates all refresh tokens — user must re-login on all devices. | Integration | — |

---
*For Permission Matrix key and TC reference definitions, see the master spec index.*
