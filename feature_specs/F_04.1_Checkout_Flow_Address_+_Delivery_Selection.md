# HOMESTYLE — Feature Specification
*v2.1 · Confidential · Derived from BRD v4.0 — Premium Design Furniture*

---

## M04 — Checkout & Payment

The checkout and payment module handles the full purchase completion flow — from TC-13 inventory hold creation through payment processing and order creation. It is the most business-critical module: every friction point here directly reduces revenue.

---

### F-04.1 Checkout Flow (Address + Delivery Selection)

**Summary**

Multi-step checkout collecting delivery address and method — including white-glove delivery selection for large furniture. Creates TC-13: 15-minute inventory hold and enforces TC-14: 30-minute checkout session hard expiry. Lead times are locked per configured item at checkout initiation.

**Background / Context**

For premium design furniture, checkout carries additional complexity vs. standard e-commerce: white-glove delivery eligibility must be surfaced and selected here, lead times must be displayed and locked per configuration, and the inventory hold timer must be prominent given the high-consideration purchase behaviour.

**User Story**

As a customer ready to buy, I want a clear checkout where I enter my delivery address, choose my delivery method (including white-glove if available), and see confirmed lead times so I can complete my purchase confidently.

**Flow — Happy Path**

1. Customer clicks 'Proceed to Checkout' from cart.
2. TC-13: System creates `inventory_hold` records in Redis (atomic SETNX) for each cart item. TC-14: 30-min checkout session created in DB.
3. Step 1 — Address: customer enters/selects delivery address. System validates serviceability.
4. Step 2 — Delivery: system returns available methods with prices, estimated dates, and white-glove option if eligible. Lead time per configured item displayed and locked.
5. Customer selects method → summary shown (items, lead times, shipping, estimated tax) → proceeds to payment (F-04.2).
6. TC-13: Countdown timer shown when ≤5 minutes remain.

**Flow — Edge Cases**

| Edge Case | Expected System Behaviour |
|---|---|
| TC-13: Hold expires (15 min) | Hold released. 'Your reservation expired.' Redirect to /cart. |
| TC-14: Full session expires (30 min) | 'Your checkout session expired. Please start again.' Redirect to /cart. All holds released. |
| Item goes out of stock between hold and payment | Caught at PaymentIntent creation. Item-level error shown. Return to cart. |
| Customer navigates away mid-checkout | Hold remains active until TC-13 TTL. Customer can return within window. |
| White-glove only available for some items | Split delivery option offered: white-glove for large items + standard for small accessories. |

**Business Rules**

| BR # | Rule |
|---|---|
| BR-01 | TC-13: Inventory hold via Redis SETNX (atomic). TTL: 15 minutes. Other customers' checkout for same configuration sees reduced available qty. |
| BR-02 | TC-14: 30-min checkout session enforced server-side. Prevents stale prices, lead times, and tax on long-idle checkouts. |
| BR-03 | White-glove delivery: product `weight_kg` > admin-configured threshold AND destination ZIP in white-glove service zone (CR-11). |
| BR-04 | Lead time per configured item locked at checkout session creation. Displayed prominently in delivery step and order summary. |
| BR-05 | Guest checkout proceeds without account if OD-01 approved. Guest order linked to email provided. |

**Input Validation Rules**

| Field | Type | Required | Validation Rules | Error Message Shown |
|---|---|---|---|---|
| addressLine1 | String | Yes | Min 5, max 200 chars. | 'Address is required' |
| city | String | Yes | Min 2, max 100 chars. | 'City is required' |
| zip / postalCode | String | Yes | US: 5 digits or ZIP+4. EU: 4-6 digits. UK: standard postcode. Validated against `serviceable_areas`. | 'Please enter a valid ZIP/postal code' / 'Delivery not available to this address' |
| country | Enum (ISO 3166-1 alpha-2) | Yes | Must be in supported countries list (US + EU/UK). | 'Please select a supported country' |
| deliveryMethodId | UUID | Yes (step 2) | Must be a valid delivery method returned by calculate-totals API for this address. | 'Please select a delivery method' |

**Permission Matrix**

| Method | Endpoint | Description | Guest | Customer | Trade | Ops | Content | Super Admin |
|---|---|---|---|---|---|---|---|---|
| **POST** | /api/v1/checkout/session | Create checkout + inventory holds | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| **PUT** | /api/v1/checkout/:sessionId/address | Set delivery address | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| **PUT** | /api/v1/checkout/:sessionId/delivery | Select delivery method | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |

**UI / Display Rules**

| Condition / State | UI Component | Display Text | Colour / Style | Available Action |
|---|---|---|---|---|
| Checkout begins — TC-13 hold created | Step indicator + hold timer (hidden until ≤5 min) | Steps: Address → Delivery → Payment | Premium minimal step indicator | — |
| TC-13: ≤5 minutes remaining | Amber countdown timer | 'Your items are reserved for [M:SS]' | Amber text with clock icon | Urgency messaging |
| TC-13: Hold expires | Full-page expiry modal | 'Your reservation expired. Items returned to your cart.' | Red modal | 'Return to Cart' button |
| Lead time shown in delivery step | Per-item lead time summary | 'Delivery includes lead time: [X] weeks for [product name]' | Informational — grey text | — |
| White-glove delivery option | Delivery method radio | 'White-Glove: Room of choice + packaging removal. Est. [date] — $[X]' | Premium option — gold left border | Selectable radio |

**API Contract**

| Method | Endpoint | Description | Request Body / Params | Response |
|---|---|---|---|---|
| **POST** | /api/v1/checkout/session | Create session + holds | `{ cartId }` | `200: { sessionId, expiresAt (TC-14), holdExpiresAt (TC-13), leadTimes[] }` |
| **PUT** | /api/v1/checkout/:sessionId/address | Set address | `{ address }` | `200: { deliveryMethods[], estimatedDates[] }` |
| **PUT** | /api/v1/checkout/:sessionId/delivery | Select method | `{ methodId }` | `200: { orderSummary }` |

**TC References:** TC-13 (15-min inventory hold), TC-14 (30-min checkout session), TC-17/18 (delivery date estimate calculation)

**Acceptance Criteria**

| # | Criteria | Test Type | Status |
|---|---|---|---|
| AC-01 | TC-13: Inventory hold created atomically — concurrent checkout for same last-in-stock configuration succeeds for only one customer. | Integration | — |
| AC-02 | TC-13: Countdown timer appears at ≤5 minutes. | E2E | — |
| AC-03 | Lead times per configuration displayed and locked in delivery step. | E2E | — |
| AC-04 | TC-14: Session expires after 30 minutes regardless of activity. | Integration | — |
| AC-05 | White-glove option only shown for qualifying product + ZIP combinations. | Integration | — |

---
*For Permission Matrix key and TC reference definitions, see the master spec index.*
